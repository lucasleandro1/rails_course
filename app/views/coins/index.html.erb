<p style="color: green"><%= notice %></p>

<h1>Moedas 🪙</h1>
<h2><%= cookies[:curso]%></h2>


<div id="coins">
<table>
  <thead>
    <tr>
      <th><%=@coins.human_attribute_name(:description)%></th>
      <th><%=@coins.human_attribute_name(:acronym)%></th>
      <th><%=@coins.human_attribute_name(:url_image)%></th>
    </tr>
  </thead>
  <tbody>
    <% @coins.each do |coin| %>
      <tr>
        <td><%= coin.description %></td>
        <td><%= coin.acronym %></td>
        <td><%= image_tag coin.url_image, width:25, height:25 %></td>
        <td><%= link_to "Show", coin %></td>
        <td><%= link_to "Edit", edit_coin_path(coin) %></td>
        <td><%= link_to "delete", coin, method: :delete, data: { confirme: "Tem certeza?" }%></td>
      </tr>
    <% end %>
  </tbody>
</table>
  
</div>

<%= link_to "New coins", new_coin_path %>
